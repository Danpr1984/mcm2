{
  "name": "my-app",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "express": "^4.17.1"
  }
}
